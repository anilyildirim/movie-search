import React from "react";

const MovieDetails = (props) => {
  return(
    <div>
      <button onClick={props.closeMovieInfo}>This is test for movie detail page</button>
    </div>
  )
}

export default MovieDetails;